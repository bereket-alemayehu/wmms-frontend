/**
 * Ticket Types Entry Point
 * Re-export all ticket-related types
 */

export type {
  Ticket,
  TicketCategory,
  TicketStatus,
  CreateTicketRequest,
  UpdateTicketRequest,
  AssignTicketRequest,
  TicketsListResponse,
  TicketResponse,
  TicketFilters,
} from './ticket'
